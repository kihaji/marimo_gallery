# kubectl apply -k deploy/horizontal_k8s/
# Create the client-CA secret referenced by ingress.yaml first:
#   kubectl create secret generic gallery-client-ca --from-file=ca.crt=your-ca.pem
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - gateway.yaml
  - scheduler.yaml
  - nb-sales-dashboard.yaml
  - nb-csv-explorer.yaml
  - nb-cluster-lab.yaml
  - ingress.yaml
  - networkpolicy.yaml
